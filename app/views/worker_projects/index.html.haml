%h1 Listing worker_projects

%table
  %thead
    %tr
      %th Worker
      %th Project
      %th
      %th
      %th

  %tbody
    - @worker_projects.each do |worker_project|
      %tr
        %td= worker_project.worker
        %td= worker_project.project
        %td= link_to 'Show', worker_project
        %td= link_to 'Edit', edit_worker_project_path(worker_project)
        %td= link_to 'Destroy', worker_project, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Worker project', new_worker_project_path
